#!/bin/bash
# CONFIG_FILE="./zshenv.sh"

# if [ -f "$CONFIG_FILE" ] && [ -r "$CONFIG_FILE" ]; then
#     source "$CONFIG_FILE"
#     echo "Successfully loaded config from $CONFIG_FILE"
# else
#     echo "Warning: $CONFIG_FILE does not exist or is not readable"
#     echo "user 'Tuckr decrypt zsh' to get $CONFIG_FILE"
#     exit 1
# fi